# مستندات فنی Guided Hacking Offset Dumper

این فایل شامل جزئیات فنی و نحوه کارکرد داخلی پروژه برای توسعه‌دهندگان است.

## ساختار پروژه
پروژه از دو بخش اصلی تشکیل شده است:
1. **GHDumper Lib:** یک کتابخانه استاتیک که تمام منطق اصلی اسکن و دامپ در آن قرار دارد.
2. **Dumper Client:** یک برنامه CLI ساده که از کتابخانه استفاده می‌کند.

## نحوه کارکرد الگوی اسکن (Pattern Scanning)
دامپر از یک الگوی اسکن دو مرحله‌ای استفاده می‌کند:
- **ScanEx:** برای اسکن پروسه‌های در حال اجرا. این تابع با استفاده از `VirtualQueryEx` حافظه را تکه تکه (Region) بررسی کرده و با `ReadProcessMemory` محتوا را برای جستجو می‌خواند.
- **ScanBasic:** برای اسکن فایل‌های روی دیسک که قبلا در حافظه بارگذاری شده‌اند.

الگوها از فرمت رشته‌ای (مانند `E8 ? ? ? ?`) پشتیبانی می‌کنند که در آن `?` به عنوان Wildcard در نظر گرفته می‌شود.

## استفاده از کتابخانه در پروژه‌های دیگر
شما می‌توانید از `GHDumper.lib` در پروژه‌های خود استفاده کنید. کافیست هدر `GHDumper.h` را اینکلود کنید:

```cpp
#include "GHDumper.h"

// بارگذاری تنظیمات از JSON
nlohmann::json config = ...;

// ایجاد اسکنر
bool runtime = true;
gh::FileScanner scanner = gh::InitScanner(config, &runtime);

// اجرای اسکن
gh::Dump signatures = gh::DumpSignatures(config, scanner);
```

## جزئیات Netvar Dumping
در بازی‌های موتور سورس، دامپر با استفاده از `dwGetAllClasses` به لیست `ClientClass` دسترسی پیدا می‌کند. سپس به صورت درختی تمام `RecvTable`ها را برای پیدا کردن ویژگی (Property) مورد نظر جستجو می‌کند.
این قابلیت نیاز دارد که دامپر دقیقا با همان معماری بازی (۳۲ یا ۶۴ بیتی) اجرا شود.

## فرمت‌های خروجی
### 1. C++ Header (.hpp)
یک Namespace به نام `ghdumper` ایجاد می‌کند که شامل ثابت‌های `constexpr` برای تمام آفست‌ها است. همچنین زمان آخرین دامپ را به عنوان `timestamp` ذخیره می‌کند.

### 2. Cheat Engine (.ct)
این فایل یک XML استاندارد است. دامپر از یک سیستم قالب‌بندی (Template) برای ایجاد گروه‌های Local Player و Entity List استفاده می‌کند.

### 3. ReClass.NET (.rcnet)
این فرمت در واقع یک فایل ZIP است که شامل یک فایل `Data.xml` می‌باشد. دامپر کلاس‌های Netvar را به گره‌های ReClass (مانند `FloatNode`, `Vector3Node` و غیره) تبدیل می‌کند.

## توسعه و مشارکت
برای افزودن قابلیت‌های جدید:
1. منطق جدید را در `src/dumper-lib` پیاده‌سازی کنید.
2. فرمت‌کننده‌های جدید را در `GHDumper.cpp` اضافه کنید.
3. در صورت نیاز به تغییر در CLI، `src/dumper/main.cpp` را ویرایش کنید.

# راهنمای اجرای پروژه Guided Hacking Offset Dumper

این پروژه یک ابزار مدرن برای استخراج آفست‌ها (Offsets) و امضاهای (Signatures) بازی‌ها است که با هر بازی‌ای سازگار است. این ابزار می‌تواند به سرعت هدرهای آفست کامل را برای هر بازی از روی دیسک یا در زمان اجرا (Runtime) تولید کند.

## پیش‌نیازها
برای کامپایل و اجرای این پروژه به موارد زیر نیاز دارید:
- **Visual Studio 2019 یا جدیدتر**
- نصب بودن پکیج **Desktop development with C++** در Visual Studio

## مرحله ۱: بیلد (Build) کردن پروژه
1. ابتدا پروژه را از گیت‌هاب کلون کنید یا فایل‌های آن را دانلود کنید.
2. فایل `GH-Offset-Dumper.sln` را با Visual Studio باز کنید.
3. پیکربندی (Configuration) را روی `Release` تنظیم کنید.
4. پلتفرم را بر اساس بازی هدف خود انتخاب کنید:
   - اگر بازی ۶۴ بیتی است، پلتفرم را روی `x64` قرار دهید.
   - اگر بازی ۳۲ بیتی است، پلتفرم را روی `x86` قرار دهید.
5. از منوی بالا، `Build > Build Solution` را انتخاب کنید.
6. فایل‌های اجرایی نهایی در مسیر `Release/` یا `x64/Release/` ساخته می‌شوند.

## مرحله ۲: نحوه استفاده (استخراج از بازی در حال اجرا)
1. بازی مورد نظر خود را اجرا کنید.
2. فایل اجرایی ساخته شده (مثلاً `GH-Offset-Dumper-64.exe`) را پیدا کنید.
3. فایل تنظیمات خود را (مانند `configs/config.json`) با موس بگیرید و روی فایل `exe` رها کنید (Drag and Drop).
4. ابزار شروع به اسکن حافظه بازی کرده و آفست‌ها را استخراج می‌کند.
5. نتایج در یک پوشه جدید که به نام بازی ساخته می‌شود، ذخیره خواهند شد.

## مرحله ۳: نحوه استفاده (استخراج از فایل روی دیسک)
اگر می‌خواهید بدون اجرای بازی، آفست‌ها را مستقیماً از روی فایل‌های `.exe` یا `.dll` استخراج کنید:
1. فایل تنظیمات (مثلاً `configs/diskSample.json`) را باز کنید.
2. مطمئن شوید مقدار `"fileonly"` برابر `true` باشد.
3. مسیر کامل فایل بازی را در فیلد `"exefile"` وارد کنید.
4. اگر ماژول‌های دیگری (DLL) برای اسکن مد نظر دارید، آن‌ها را در بخش `"additionalModules"` لیست کنید.
5. فایل JSON تنظیمات را روی فایل `exe` داسپر رها کنید.

## ساختار فایل تنظیمات (JSON)
فایل تنظیمات شامل بخش‌های زیر است:
- `"executable"`: نام پروسه بازی (مثلاً `notepad.exe`).
- `"filename"`: نام پایه برای فایل‌های خروجی.
- `"signatures"`: لیستی از امضاها که شامل نام و الگوی بایت‌ها (`pattern`) است.

## فایل‌های خروجی
پس از پایان عملیات، این فایل‌ها تولید می‌شوند:
1. **فایل `.hpp`**: یک هدر فایل C++ شامل تمام آفست‌ها که می‌توانید مستقیماً در پروژه خود include کنید.
2. **فایل `.ct`**: جدول Cheat Engine که آفست‌ها را در چیت انجین نمایش می‌دهد.
3. **فایل `.rcnet`**: فایل مخصوص ReClass.NET برای بازسازی ساختارهای داده.

---
*نکته: اگر بازی از موتور Source استفاده می‌کند، حتماً از نسخه متناسب (۳۲ یا ۶۴ بیتی) استفاده کنید تا Netvarها به درستی استخراج شوند.*
